{
  "name": "web-admin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/web-admin/src",
  "tags": ["scope:web", "type:app"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "vite --config apps/web-admin/vite.config.ts" }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "vite build --config apps/web-admin/vite.config.ts" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -p apps/web-admin/tsconfig.app.json --noEmit" }
    }
  }
}
